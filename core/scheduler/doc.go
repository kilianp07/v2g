package scheduler

// Package scheduler implements day-ahead planning for NEBEF effacement.
// It builds per-vehicle dispatch plans respecting availability and
// power constraints. Plans can be exported to JSON or CSV.
